<h1>Converted Temperatures</h1>

<table>
  <tr>
    <th>Original Value</th>
    <th>Converted Value</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @temperatures.each do |temperature| %>
  <tr>
    <td><%= temperature.labeled %></td>
	<td><%= temperature.labeled_conversion %></td>
    <td><%= link_to 'Show', temperature %></td>
    <td><%= link_to 'Edit', edit_temperature_path(temperature) %></td>
    <td><%= link_to 'Destroy', temperature, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Conversion', new_temperature_path %>
